# 静态审查记录

## 关注点

- 框架层不得引用 `Autodesk.Revit` 命名空间。
- Revit API 只能出现在 `PlugHub.Revit2020` 适配层。
- 样例模块用于验证模块契约和配置组合，不实现真实业务操作。
- 配置变更后必须运行 C# 静态验证器。

## 当前结论

C# 静态验证器覆盖项目结构、分层约束、配置关系、视图组合和 Revit Ribbon 适配入口。Revit 实机加载仍需在 Revit 2020 中验证。
